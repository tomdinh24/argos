# Runtime dependencies for the FastAPI backend, mirrored from pyproject.toml
# [project.dependencies]. This file exists so Railway's Nixpacks builder has a
# deterministic install target (it always detects requirements.txt); the package
# itself is made importable at runtime via PYTHONPATH=src in railway.json's
# startCommand, so no `pip install .` build step is required.
#
# NOTE: argos_live_sdk (the Foundry OSDK) is intentionally absent — it is a
# private wheel, lazily imported, and only loaded when ARGOS_FOUNDRY_BRIDGE_ENABLED=1.
# Keep the bridge OFF on Railway until the OSDK is re-pinned to ontology 88f01e1f
# (see docs/DECISIONS.md 2026-06-07). When you add it, install the wheel separately.
#
# Keep in sync with pyproject.toml.
pydantic>=2.7
anthropic>=0.40
openai>=1.50
duckdb>=1.5
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
python-dotenv>=1.0
